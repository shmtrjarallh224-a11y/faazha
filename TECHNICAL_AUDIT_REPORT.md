# FAZAAH / فزعة — Technical Audit Report

## نطاق المراجعة

تمت مراجعة مسارات المصادقة، فصل الأدوار، الطلبات، الطوارئ، الرسائل، الإشعارات، ملفات المهنيين، لوحة الإدارة، وعقود OpenAPI وقاعدة البيانات الحالية.

## الإصلاحات المنفذة

### الهوية وتجربة المستخدم

- توحيد الاسم التجاري الظاهر للمستخدم إلى **فزعة | FAZAAH** في شاشة البداية، العناوين، الوصف، لوحة الإدارة، الإعدادات، والرسائل.
- الحفاظ على دعم RTL واللغتين العربية والإنجليزية في هوية المنتج.
- إضافة لوحة مهنية مستقلة مرتبطة بالطلبات والملف المهني الحقيقي.
- فصل شريط التنقل وتجربة العميل عن تجربة المهني.
- حفظ نوع الحساب المختار من شاشة البداية عند التسجيل بالبريد أو الهاتف.

### المصادقة والأمان

- استبدال التوكن غير الموقّع بتوكن HMAC موقّع باستخدام `SESSION_SECRET` مع مدة صلاحية 30 يوماً.
- دعم التحقق من التوقيع والزمن قبل قبول التوكن.
- الحفاظ على التوافق مع كلمات المرور القديمة أثناء الانتقال.
- إصلاح تدفق OTP بحيث لا يتم استهلاك الرمز قبل إكمال تسجيل المستخدم الجديد.
- إضافة إنشاء ملف مهني تلقائي عند تسجيل مستخدم مهني عبر OTP.
- إضافة حماية أدوار على إنشاء الطلبات وتعديلها وعرضها.

### الطلبات والطوارئ

- قصر إنشاء طلبات الخدمة على حسابات العملاء.
- قصر قبول وبدء وإكمال الطلبات على المهني المرتبط بالطلب.
- السماح للعميل بإلغاء طلبه فقط.
- إصلاح عداد الأعمال المكتملة ليزداد فعلياً بدلاً من تخزين معرّف الطلب.
- ربط الطوارئ بمهني متاح فعلياً من قائمة المهنيين، مع إنشاء طلب مطابق لعقد API.
- إضافة إشعارات قاعدة البيانات عند إنشاء الطلب وتغيير حالته.

### الرسائل

- منع قراءة محادثة أو الكتابة فيها من مستخدم ليس طرفاً فيها.
- احتساب الرسائل غير المقروءة من الطرف الآخر فقط.
- إنشاء إشعار حقيقي عند إرسال رسالة.

### ملف المهني

- إضافة `GET /api/providers/me` للعثور على الملف المهني بواسطة المستخدم المصادق عليه.
- إصلاح تحديث حالة التوفر لاستخدام معرّف ملف المهني، وليس معرّف المستخدم.
- إضافة لوحة مهني تعرض الطلبات، الحالات، الأعمال المكتملة، التقييم، الخبرة، التوفر، وحالة التوثيق.

### العقود والجودة

- تحديث OpenAPI وإعادة توليد React Query hooks وZod schemas.
- نجاح فحص TypeScript الكامل عبر الحزم.
- نجاح فحص صحة API عبر `/api/healthz`.

## حدود ما يزال يحتاج تكاملات إطلاق خارجية

هذه العناصر لا يمكن تفعيلها تجارياً من داخل الكود وحده دون حسابات وخدمات خارجية:

- إرسال OTP عبر مزود SMS فعلي.
- إشعارات FCM على أجهزة Android وiOS.
- رفع الصور والوثائق إلى Object Storage دائم وربطها بسجلات التوثيق.
- بوابة الدفع والمحفظة والسحب والعمولات.
- مؤشرات الكتابة والقراءة الفورية عبر WebSocket أو خدمة Realtime.
- بناء تطبيق Flutter أصلي ونشره على متجري Google Play وApp Store.

## توصية الإطلاق

النسخة الحالية أصبحت أساساً عملياً قابلاً للتوسع على الويب مع API وPostgreSQL وتدفقات أدوار حقيقية. قبل الإطلاق التجاري النهائي، يجب إكمال التكاملات الخارجية المذكورة، إضافة اختبارات تكامل للمصادقة والطلبات والرسائل، ثم إعداد بيئة إنتاج منفصلة وترحيلات قاعدة البيانات.
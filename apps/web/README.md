# @workspace/web

This directory contains a minimal Next.js (App Router) + TypeScript web app scaffold for the Faazha monorepo.

To run:

1. From repository root: `pnpm install`
2. `pnpm --filter @workspace/web dev` or `cd apps/web && pnpm install && pnpm dev`
